"use strict";

var TAG_PATTERN = /^\s*@[^\s]+/;

module.exports = {
  isEmptyLine: function isEmptyLine(line) {
    return (/^\s*$/.test(line)
    );
  },
  isTagLine: function isTagLine(line) {
    return TAG_PATTERN.test(line);
  }
};